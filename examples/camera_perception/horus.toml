[package]
name = "camera_perception"
version = "0.1.0"
description = "Camera perception pipeline — image capture, object detection, tracking"
authors = ["softmata"]
license = "Apache-2.0"

[dependencies]
horus = "*"
