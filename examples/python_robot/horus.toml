[package]
name = "python_robot"
version = "0.1.0"
description = "Differential-drive robot in Python — the same example as differential_drive but using Python API"
authors = ["softmata"]
license = "Apache-2.0"

[dependencies]
horus = "*"
