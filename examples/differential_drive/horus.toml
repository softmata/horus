[package]
name = "differential_drive"
version = "0.1.0"
description = "Differential-drive mobile robot — basic CmdVel control with sim3d"
authors = ["softmata"]
license = "Apache-2.0"

[dependencies]
horus = "*"
