[package]
name = "multi_robot"
version = "0.1.0"
description = "Multi-robot formation — launch files, namespaces, and discovery"
authors = ["softmata"]
license = "Apache-2.0"

[dependencies]
horus = "*"
