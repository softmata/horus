[package]
name = "horus_perception"
version = "0.1.8"
edition = "2021"
description = "AI-friendly perception types for HORUS - Detection, PointCloud, Landmarks"
license = "MIT"

[dependencies]
horus_core = { workspace = true }
bytemuck = { workspace = true }
thiserror = { workspace = true }
