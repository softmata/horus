name: snakesim
version: "0.1.6"
description: "Snake game controller - converts keyboard and joystick input to snake state"
author: "HORUS Team"
license: "Apache-2.0"
session_id: "snakesim-session"

dependencies:
  - horus
  - name: serde
    version: "1"
    features:
      - derive

features:
  - input-nodes  # Enables keyboard and joystick input
